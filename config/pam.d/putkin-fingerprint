# Separate conversation: scanning never blocks entry of a password.
# A lock can outlive the default 30 seconds. Keep waiting until a scan, the
# attempt limit, password success or suspend cancels this conversation.
auth required pam_fprintd.so max-tries=3 timeout=-1
