# Shared behavior for the homelab aliases from ~/.ssh/config.
share_connections yes
shell_integration inherited
forward_remote_control no

hostname radiology traefik-lan traefik-wan mediarr security radiodev
login_shell fish
copy .config/fish/config.fish .config/fish/conf.d/aliases.fish .config/starship.toml .tmux.conf
