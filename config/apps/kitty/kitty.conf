include current-theme.conf

font_family JetBrainsMono Nerd Font Mono
bold_font        auto
italic_font      auto
bold_italic_font auto

font_size 12

# Play the terminal bell directly: Hyprland does not play the system bell.
enable_audio_bell yes
# Native background transparency keeps terminal text fully opaque.
# Background blur is provided by Hyprland.
background_opacity 0.90
dynamic_background_opacity yes

# Try Fish in Kitty without changing the system login shell.
shell /usr/bin/fish

# Browse scrollback in Neovim: y copies to the system clipboard, q exits.
scrollback_pager nvim -u NONE -i NONE --cmd 'set eventignore=FileType' +'nnoremap q ZQ' +'call nvim_open_term(0, {})' +'set nomodified nolist clipboard=unnamedplus' +'$' -

map ctrl+shift+t new_tab_with_cwd
map ctrl+shift+enter new_window_with_cwd
